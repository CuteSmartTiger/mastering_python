from tkinter import *

# 创建窗口对象的背景色
root = Tk()

Label(root, text='VM INFO', bg='white').pack(side=LEFT, anchor=W, fill=X, expand=YES)

root.title('SHOW DATA')
root.geometry("400x100")
root.mainloop()
